# cynthialy LP — environment variables template
# ----------------------------------------------------------------
# 使い方:
#   cp .env.example .env
#   # 下の値を実際のキーに書き換える
#   # .env は .gitignore に含まれるのでリポジトリに上がらない
# ----------------------------------------------------------------

# OpenAI API key（画像生成スクリプト用、scripts/generate-images.mjs）
# 取得：https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
